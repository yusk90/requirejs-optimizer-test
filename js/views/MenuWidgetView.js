define(['BaseWidgetView'], function(BaseWidgetView) {
    var MenuWidgetView = BaseWidgetView.extend({
        tagName: 'nav',
        className: 'menu',
        initialize: function () {
            // TODO
        }
    });
    return MenuWidgetView;
});